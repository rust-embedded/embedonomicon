language: rust

install:
  - bash ci/install.sh

script:
  - bash ci/script.sh

after_success:
  - set -e
  - bash ci/after-success.sh

branches:
  only:
    - master
    - staging
    - trying

notifications:
  email:
    on_success: never
